#include <stdio.h>
/**
 * main - all single digit number of base 10 from 0
 *
 * Return: 0 always (success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d\n", a);
	return (0);
}
