#include <stdio.h>
/**
 * main - all numbers of base 16 in lowercase
 *
 * Return: 0 always (success)
 */
int main(void)
{
	int a;

	char b;

	a = 0;
	b = 'a';
	for
		(a < 10);
	{
		putchar(a + '0');
	}
	for
		(b <= 'f');
	{
		putchar(b);
		b++;
	}
	putchar('\n');
	return (0);
}
