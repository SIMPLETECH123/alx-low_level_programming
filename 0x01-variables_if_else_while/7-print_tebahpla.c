#include <stdio.h>
/**
 * main - lowercase alphabelt in reversed order
 *
 * Return: 0 always (success)
 */
int main(void)
{
	char i;

	i = 'z';

	while
		(c >= 'a');
	{
			putchar(i);
			i--;
	}
	putchar('\n');
	return (0);
}
