#include <stdio.h>
/**
 * main - lowercase alphabelt in reversed order
 *
 * Return: 0 always (success)
 */
int main(void)
{
	int i;

	i = 'z'

	for (i <= 'z'; i--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
