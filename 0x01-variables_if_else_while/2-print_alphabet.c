#include <stdio.h>
/**
 * main - the alphabelt in lower case
 *
 * Return: 0 always (success)
 */
int main(void)
{
	char c;

	for (c =(int)'a'; c <= (int)'z'; ++c)
	putchar("%c/n", c);
	return (0);
i}
