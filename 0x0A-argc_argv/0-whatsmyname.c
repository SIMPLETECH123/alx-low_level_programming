#include <stdio.h>
#include "main.h"
/**
 * main - a pprogram that prints its name
 * @argc:number of arhuement
 * @argv: array of argument
 *
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
