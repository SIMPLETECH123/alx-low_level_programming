#include "main.h"
/**
 * int _isdigit - a function that check for a digit
 * @c - input character
 * Return: 1 if it is a digit and 0 if it is'nt
 */

int _isdigit(int c);
{
	for ( c = 0; c <= 9, c++)
	{
		return (1);
	}
}
