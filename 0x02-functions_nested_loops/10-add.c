#include "main.h"
/**
 * add -add two integers and return the result
 *
 * @a: number one
 * @b: number two
 *
 * Return: add of both numbers
 */
int add(int a, int b)
{
	int n = a + b;

	printf("%d", n);
	return (0);
}
